#!/usr/bin/env sh

set -e

echo "Building data.json"
npx dai2json data/PresLib_e4.0.0.dai data.json

npx vite build

mkdir -p sprites
echo "Building sprites"
for MODE in day dusk night; do
  spreet symbols/$MODE sprites/$MODE
  spreet --retina symbols/$MODE sprites/$MODE@2x
done
